# Experiment 4

Boundary task steering on frozen GR00T policies.

This experiment studies whether a fixed delta at the System 2 -> System 1
boundary can redirect a frozen GR00T policy, and what variable that delta
actually controls.

Current read:

- the DAgger-style teacher-relabeling protocol still makes sense for this work
- the six-layout Run 81 matrix is the main result now
- position steering transfers more cleanly than object steering
- the next branch should restart from a cleaner factorization setup rather than
  keep iterating the same recipe
